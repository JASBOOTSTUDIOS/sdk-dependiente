# memoria_neuronal (compilador)

Fuentes JMN embebidas en **jas-compiler-c** para lo que el compilador necesite enlazar o referenciar.

La JMN del **runtime estable** (VM) está en **`sdk-dependiente/jasboot-jmn-core/`** y la enlaza **`jasboot-ir`**.
